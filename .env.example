GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
OAUTH_REDIRECT_URI=http://127.0.0.1:8000/auth/google/callback
OWNER_EMAIL=
SESSION_SECRET=
# Production only: set to "true" when serving over HTTPS (origin sees X-Forwarded-Proto=https)
SESSION_HTTPS_ONLY=false
